<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.jingbao.dao.LoginDao">
    <resultMap id="LoginMap" type="User">
        <id column="customer_id" property="customerId"/>
        <result column="password" property="password"/>
        <result column="username" property="username"/>
        <association property="customer" column="customer_id" select="customer"/>
    </resultMap>
    <select id="validation" resultMap="LoginMap" parameterType="java.lang.String">
        SELECT username, password, customer_id
        from t_user
        where username = #{username}
    </select>

    <resultMap id="CustomerMap" type="Customer">
        <id column="customer_id" property="customerId"/>
        <result column="customer_name" property="customerName"/>
    </resultMap>

    <select id="customer" resultMap="CustomerMap" parameterType="java.lang.Integer">
        SELECT customer_name, customer_id
        from t_customer
        where customer_id = #{customer_id}
    </select>
    <insert id="register" parameterType="User">
        insert into t_user(username, password) value (#{user.username},#{user.password} )
    </insert>
</mapper>
